package user

import (
	"github.com/ouczbs/Zmin/engine/zclass"
	"mmo/engine/conf"
)

type (
	TSequence = conf.TSequence
	TSize = conf.TSize
	TCode = conf.TCode
	TCmd = conf.TCmd
	TEnum =conf.TEnum
	TComponentId = conf.TComponentId

	UStackPool = zclass.UStackPool
)
